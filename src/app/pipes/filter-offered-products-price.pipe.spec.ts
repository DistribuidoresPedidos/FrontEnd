import { FilterOfferedProductsPricePipe } from './filter-offered-products-price.pipe';

describe('FilterOfferedProductsPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOfferedProductsPricePipe();
    expect(pipe).toBeTruthy();
  });
});
